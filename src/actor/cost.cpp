
#include "mess2_algorithm_plugins/actor/cost.hpp"

namespace mess2_algorithms
{
    // std::vector<double> generate_costs(const Graph& graph)
    // {
    //     const auto edges = graph.get_edges_();
    //     const auto vertices = graph.get_vertices_();
    //     for (int64_t iter = 0; iter < static_cast<int64_t>(edges.size()); ++iter)
    //     {
    //         const auto edge = edges[iter];
    //         const auto vertex_parent = vertices[edge.get_index_parent_()];
    //         const auto vertex_child = vertices[edge.get_index_child_()];

    //         const auto type = edge.get_type_();
    //         if (type=="wait") {
                
    //         }
    //     }
    // }

} // namespace mess2_algorithms
